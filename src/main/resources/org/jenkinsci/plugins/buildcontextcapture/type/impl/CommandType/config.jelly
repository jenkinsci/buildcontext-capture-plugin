<j:jelly xmlns:j="jelly:core" xmlns:f="/lib/form">

    <f:entry title="${descriptor.label}" help="/descriptor/${descriptor.typePackageName}/help/type">

        <f:entry field="scriptContent" title="${%Script content}">
            <f:textarea/>
        </f:entry>

        <f:entry field="filePath" title="${%File Path}">
            <f:textbox/>
        </f:entry>

    </f:entry>

</j:jelly>